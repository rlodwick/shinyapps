This Shiny app is a random score generator for football matches.

It assumes that the number of goals scored by each team is random, 
from a Poisson(lambda) distribution. 

Both lambda and the number of matches can be specified by the user. 
